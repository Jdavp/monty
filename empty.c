#include "monty.h"
/**
 * empty - fn print error when not find functios allow
 * Return: EXIT_FAILURE
 */
int empty(void)
{
	fprintf(stderr, "L%d: unknown instruction %s\n", e.nline, e.command);
	return (EXIT_FAILURE);
}
